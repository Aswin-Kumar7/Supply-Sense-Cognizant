fastapi>=0.136.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.23
asyncpg==0.29.0
aiosqlite==0.20.0
alembic>=1.13.0
pydantic>=2.9.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
sse-starlette>=2.1.0
httpx>=0.27.0
boto3>=1.34.0
# ── AI agent stack ───────────────────────────────────────────────
# LangGraph powers the conversational advisor (app/agents/langgraph_advisor.py).
langgraph>=1.2.0
langchain-aws>=1.6.0
langchain-core>=1.4.0
# Strands powers the event-processing supervisor pipeline (app/agents/strands_agents.py).
# Was installed ad hoc in the venv but missing here — a fresh deploy would disable
# those agents without it.
strands-agents>=1.45.0
